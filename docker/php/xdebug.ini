; Development only. `debug` mode is off by default so the dev container keeps
; production-like speed; flip XDEBUG_MODE=debug when you actually need to step.
xdebug.mode = off
xdebug.start_with_request = trigger
xdebug.client_host = host.docker.internal
xdebug.client_port = 9003
